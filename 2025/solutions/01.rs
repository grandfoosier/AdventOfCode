pub fn run(input: &str) {
    println!("Running 2025 Day 01 solution (sample)");
    println!("Input length: {} bytes", input.len());
    if let Some(first) = input.lines().next() {
        println!("First line: {}", first);
    }
}
